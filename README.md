# DN9DKN QRZ page (responsive map image)

This version shows only a local image (QRZ-safe).

Change requested:
- The map image uses the same width as the content.
- Height adjusts automatically (responsive), no fixed aspect-ratio box.

Files:
- `index.html`
- `img/wavelog.png`
